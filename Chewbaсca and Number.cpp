#include<bits/stdc++.h>
using namespace std;
#define migmig ios::sync_with_stdio(false);cin.tie(0);cout.tie(0);
int main(){
	migmig;
	int a;
	string b;
	cin>>a;
	b=to_string(a);
	b.reverse();
	a=stoi(b);
	while(a){
		b=a%10;
		if(b<=4){
			cout
		}
	}

	return 0;
}
