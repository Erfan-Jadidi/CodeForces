#include<bits/stdc++.h>

using namespace std;

#define ll long long int
#define flash ios::sync_with_stdio(false);cin.tie(0);cout.tie(0);
#define endl "\n"

int main(){
	flash;
	int t, size, value, temp;
	cin >> t;
	while( t-- )
	{
		cin >> size >> value;
		queue<int> arr;
		while( size-- )
		{
			cin >> temp;
			arr.push(temp);
		}
		
	}

	return 0;
}
